version: 2.1
orbs:
  gcp-gke: circleci/gcp-gke@1.0.3
  helm: circleci/helm@0.2.3

jobs:
  k8s_deploy:
    working_directory: ~/GaloyApp/backend
    docker:
      - image: kiwigrid/gcloud-kubectl-helm:latest
    steps:
      - run:
          name: authorize gloud on gke
          command: |
            echo ${GCLOUD_SERVICE_KEY} > gcloud-service-key.json
            gcloud auth activate-service-account --key-file=gcloud-service-key.json
            gcloud container clusters get-credentials devnet-cluster --zone ${GOOGLE_COMPUTE_ZONE} --project ${GOOGLE_PROJECT_ID}
      - run:
          name: wait for k8s worker nodes
          command: echo $(pwd) && sleep 30 # It’s a place for improvement
      - run:
          name: helm install
          command: |
            helm install bitcoind-chart \
            -f ~/GaloyApp/backend/bitcoind-chart/regtest-values.yaml \
            ~/GaloyApp/backend/bitcoind-chart
      - run:
          name: Check Helm release status
          command: helm list --all-namespaces --all



workflows:
  kube:
    jobs:
      # - gcp-gke/create-cluster:
      #     cluster: devnet-cluster
      #     additional-args: '--num-nodes=1'
      - gcp-gke/create-node-pool:
          cluster: devnet-cluster
          additional-args: '--num-nodes=1 --machine-type=n1-standard-1 --disk-size=10Gi'
          node-pool: devnet-nodepool
          # requires:
          #   - gcp-gke/create-cluster
      - k8s_deploy:
          requires:
            - gcp-gke/create-node-pool





# # Javascript Node CircleCI 2.0 configuration file
# #
# # Check https://circleci.com/docs/2.0/language-javascript/ for more details
# #
# version: 2
# jobs:
#   build:
#     docker:
#       # specify the version you desire here
#       - image: circleci/node:8

#       # Specify service dependencies here if necessary
#       # CircleCI maintains a library of pre-built images
#       # documented at https://circleci.com/docs/2.0/circleci-images/
#       # - image: circleci/mongo:3.4.4

#     working_directory: ~/GaloyApp/mobile

#     steps:
#       - checkout:
#           path: ~/GaloyApp

#       # Download and cache dependencies
#       - restore_cache:
#           keys:
#             - v1-dependencies-{{ checksum "package.json" }}
#             # fallback to using the latest cache if no exact match is found
#             - v1-dependencies-

#       - run: yarn install

#       - save_cache:
#           paths:
#             - node_modules
#           key: v1-dependencies-{{ checksum "package.json" }}

#       # run tests!
#       - run: ls
#       - run: cd yarn test