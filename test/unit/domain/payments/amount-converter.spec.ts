import { toSats } from "@domain/bitcoin"
import { toCents } from "@domain/fiat"
import { AmountConverter } from "@domain/payments"
import { WalletCurrency } from "@domain/shared"

describe("AmountConverter", () => {
  describe("addMissingAmounts", () => {
    it("exists", () => {})
  })
})
