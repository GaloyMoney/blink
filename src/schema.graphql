type Transaction {
  # TODO add ID for caching purpose
  amount: Int!,
  description: String!,
  created_at: String!,
  hash: String,
  type: String!,
}

type Price {
  t: Int
  o: Float
}

type Earn {
  """
  the earn reward for the app to display their associated amount
  """
  id: String
  value: Int
}

type Query {
  # TODO add: version
  
  prices: [Price]
  balance(uid: String): String
  transactions(uid: String): [Transaction]

  earns: [Earn] # TODO

  # test
  getinfo: String
}

type Mutation {
  addInvoice(value: Int, memo: String, uid: String): String
  addEarn(snapshot: [String], uid: String): Boolean
  updatePendingInvoice(hash: String, uid: String): Boolean
  payInvoice(invoice: String, uid: String): String
}