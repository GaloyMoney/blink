// export const verify = async (walletId: WalletId) => {
// const path = await getPath(walletId);
// if (path instanceof Error) return path;
// const result = await verifyPath(path, walletId);
// if (result instanceof Error) return result;
// return result === true
// }

// build the tree and store the tree.

// loads up the tree.

// getProofForWalletId(Tree , WalletId) : returns the proof for the wallet id. which is returned to the front end.

// export const generateTree = async (walletId: WalletId): Tree  => {

// const wallets = await getAllWallets();
