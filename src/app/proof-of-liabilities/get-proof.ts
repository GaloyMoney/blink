// import { AccountsRepository } from "@services/mongoose"
//
// export const getProof = async (accountId: AccountId)=> {
//   const account = await AccountsRepository().findById(accountId)
//   // Add a new field nonce to the accounts
//   if (account instanceof Error) return account
//   const generateProof = await getGenerateProof(accountId,account.nonce)
//
// }
