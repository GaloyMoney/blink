[request_definition]
r = sub, res, act

[policy_definition]
p = role, scope, res, act

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.role, p.scope) && r.res == p.res && r.act == p.act
